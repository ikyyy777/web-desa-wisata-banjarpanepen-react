{
    "version": 2,
    "builds": [
        {
            "src": "server/entry-server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server/entry-server.js"
        }
    ]
}